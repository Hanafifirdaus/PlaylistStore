@model PlaylistStore.Models.GenreModel

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm())
{
    @Html.TextBoxFor(model => model.Name);
    <p>
        <button type="submit" value="Create">Create</button>
    </p>
}